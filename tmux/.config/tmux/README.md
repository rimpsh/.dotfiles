This tmux config is taken from: https://github.com/omerxx/dotfiles

## Install

Make sure TPM is installed:

```bash
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```

Run `Ctrl+I` to install the plugins
